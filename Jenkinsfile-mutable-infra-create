pipeline {
  agent any

  stages {

    stage('Repo Clone') {
      steps {
        dir('terraform') {
          git branch: 'main', url: 'https://github.com/kana-Gayatri/terraform-mutable.git'
        }
      }
    }

    stage('VPC') {
      steps {
        dir('terraform') {
         sh '''
         cd vpc
         sh 'ls -ltr'
         make dev-apply
         '''
        }
      }
    }

    }

  }


